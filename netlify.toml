[build]
  command = "npm run build"
  publish = "dist/client"

[functions]

# Note: The Netlify Vite plugin for TanStack Start handles redirects automatically
# via the generated function config, but we specify the publish directory here
# to ensure Netlify knows where the static assets (JS, CSS, images) are located.
